// _ is used to specify a private folder and this folder will be skipped from the routes
// we can name the _lib folder as %5Flib if we want /_lib to be routable
export default function Products() {
    return (
        <>
            <h1>This is not routable</h1>
        </>
    )
}